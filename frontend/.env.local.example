# Copy to `.env.local` to flip the frontend to the live backend.
# `.env.local` is git-ignored by the default Vite gitignore so local
# developer choices do not leak into the repo.
VITE_USE_MOCK=false
VITE_API_URL=http://localhost:8000
